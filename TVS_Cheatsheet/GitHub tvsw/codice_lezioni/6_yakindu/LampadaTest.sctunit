testclass LampadaTest for statechart Lampada {

	@Test
	operation test () {
		enter
		assert active (main_region.OFF)
		raise poweron
		assert active (main_region.STANDBY)
		raise pushbutton
		assert active (main_region.ON)
		assert val == 100
		exit
	}
}
