MODULE main

VAR
 a : boolean;
 switch : {ON, OFF, SPENTO};
 altezza : 0 .. 1000;
 larghezza : 0.. 200;

DEFINE
  larghezza2 :=  altezza/2 + 4 ;
  
ASSIGN
-- stato iniziale
--init(switch) := OFF;
init(switch) := {ON,OFF};
init(altezza) := 0;
-- stato successivo
next(switch) := 
	case
		a : ON;
		altezza < 30 & larghezza = 80: SPENTO;
		TRUE: {OFF, SPENTO}; -- else 
	esac;
	
next(altezza) := altezza + 1;
next(a) := !a;
-- 
larghezza := altezza/2 + 4 ;

