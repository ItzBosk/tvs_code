MODULE main

VAR
request : boolean;
status : {ready,busy};

ASSIGN
	init(status) := ready;
	next(status) := case
		request & status = busy : status;
		request : busy;
		! request: {ready,busy};
	esac;
LTLSPEC
	G(request -> F status=busy)