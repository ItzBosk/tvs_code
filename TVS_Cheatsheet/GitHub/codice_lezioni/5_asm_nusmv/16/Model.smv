MODULE main
VAR
  countsss : {a,b,c};
ASSIGN
  init(countsss) := a;
  next(countsss) := case
    countsss = a : c;
    countsss = c : b;
    countsss = b : a;
   esac; 
--SPEC
--  AG (proc1.state = entering -> AF proc1.state = critical)
